source 'https://rubygems.org'

gem "awesome_nested_set", "2.1.6"

group :development, :test do
	gem 'rake', '10.3.2'
	gem 'hirb'
	gem 'awesome_print'
	gem 'guard', '~> 2.6.1'
    gem 'guard-livereload', require: false
	gem 'guard-minitest', '~> 2.3.1'
	gem 'libnotify'
end

# redmine_gemfile = File.join(File.dirname(__FILE__), "Gemfile.local")
# if File.exists?(redmine_gemfile)
#   puts "Loading redmine Gemfile ..." if $DEBUG # `ruby -d` or `bundle -v`
#   instance_eval File.read(redmine_gemfile)
# end